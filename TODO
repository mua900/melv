- animation
- networking
- stop preloading every asset by default (memory bloat)
- fix visual glitch that happens when allocating a big gpu transfer buffer
- split the example to subsystems like draw, animation, assets etc.